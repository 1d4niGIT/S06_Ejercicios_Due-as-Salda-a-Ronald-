using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;

public class B3_15 : MonoBehaviour
{
    List<string> útiles = new List<string>();
    void Start()
    {
        útiles.Add("mochila");
        útiles.Add("pinceles");
        útiles.Add("hojas");
        útiles.Add("tajador");
        útiles.Add("pinceles");
        bool encontrado = false;
        for (int i = 0; i < útiles.Count; i++)
        {
            if (útiles[1] == útiles[i] && i != 1)
            {
                encontrado = true;
                Debug.Log($"{útiles[1]} de la posición 1 es igual al elemento {útiles[i]} de la posición {i}");
            }
        }
        if (!encontrado)
        {
           Debug.Log($"{útiles[1]} de la posición 1 es diferente a todos los elementos de la lista");
        }
        
    }

    void Update()
    {

    }
}
